diff -rupN roundup-2.1.0.orig/roundup/scripts/roundup_server.py roundup-2.1.0/roundup/scripts/roundup_server.py
--- roundup-2.1.0.orig/roundup/scripts/roundup_server.py	2021-06-14 23:36:45.000000000 +0000
+++ roundup-2.1.0/roundup/scripts/roundup_server.py	2022-01-09 22:28:04.087262448 +0000
@@ -747,7 +747,7 @@ class ServerConfig(configuration.Config)
             # This sets the socket to non-blocking. SSL needs a blocking
             # socket, so we do this only for non-SSL connections.
             if hasattr(socket, 'setdefaulttimeout'):
-                socket.setdefaulttimeout(60)
+                socket.setdefaulttimeout(300)
             base_server = http_.server.HTTPServer
 
         # obtain request server class
